return {
   id = "qe_core",
   description = [[
Quantum Baby state: contamination (a persisted resource) and humanity (computed,
never stored - see api/Resources.lua), five augmentation slots (neuro, limbs,
torso_core, spinal, ri_703) with install/uninstall, and the qe_core event bus
other QE mods hook into. Locked resource model per
docs/Quantum_Effect_Amendment_B_Engine_Pivot.md D-22: humanity and contamination
only, no Quantum Energy/Original Sin/Sync Rate.
]],
   version = "0.1.0",
   dependencies = {
      base = ">= 0"
   }
}
